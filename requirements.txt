sounddevice>=0.4.0
websockets>=12.0
numpy>=1.20.0
sherpa-onnx>=1.10   # speaker verification (owner-only mode); model downloaded separately, see README
pyserial>=3.5       # radio interface PTT (AIOC/Digirig), see radio_interfaces.py
openwakeword==0.4.0 # local wake-word ("Hey Jarvis"); pinned — newer releases require
                    # tflite-runtime, which has no published wheel for arm64/py3.13, and
                    # their Model() API renamed wakeword_models -> wakeword_model_paths
                    # (the daemon's call site matches 0.4.0 specifically)
onnxruntime>=1.14   # ONNX inference backend for openwakeword
zhconv>=1.4         # Traditional -> Simplified Chinese normalisation for transcripts/TTS
langdetect>=1.0.9   # language filter (EN/ZH-only mode drops other-language hallucinations)
