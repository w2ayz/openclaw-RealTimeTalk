__pycache__/
*.pyc
*.pyo
*.swp
.DS_Store
~/.openclaw/secrets/

# Claude Code session artifacts — local only. This repo is public, and these
# files (transcripts, worktrees, scratch scripts) must never be committed.
.claude/
